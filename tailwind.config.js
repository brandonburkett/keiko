module.exports = {
  theme: {
    extend: {
      // used for container, accounts for header / footer / border
      minHeight: {
        'mini-screen': 'calc(100vh - 12.5rem - 2px)',
      },
    },
  },
};
